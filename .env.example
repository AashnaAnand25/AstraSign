# Optional: use local sign/translate clone for 2D signing (exact same as sign.mt, then edit their repo for neon avatar)
# Leave unset to use https://sign.mt
# VITE_SIGN_VIEWER_URL=http://localhost:4200
